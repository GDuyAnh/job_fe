export default defineNuxtRouteMiddleware(() => {
  // const authStore = useAuthStore()
  // const { redirectHandler } = useAuth()
  // const user = authStore.user
  // if (user) {
  //   const result = redirectHandler(user.status)
  //   if (result.redirect && result.redirect !== ROUTE_PAGE.MY_PAGE.PROFILE) {
  //     return navigateTo(result.redirect)
  //   }
  //   if (result.showError) {
  //     authStore.logout()
  //   }
  // }
})
